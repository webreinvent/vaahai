{
  "agents": {
    "user_proxy": {
      "name": "UserProxy",
      "system_message": "You are a helpful user proxy agent that can execute code and provide feedback.",
      "human_input_mode": "ALWAYS",
      "code_execution_config": {
        "work_dir": "workspace",
        "use_docker": false
      }
    },
    "assistant": {
      "name": "Assistant",
      "system_message": "You are a helpful AI assistant that can solve problems and write code.",
      "llm_config": {
        "config_list": [
          {
            "model": "gpt-4",
            "api_key": "${OPENAI_API_KEY}",
            "api_type": "openai"
          }
        ],
        "temperature": 0.7,
        "max_tokens": 2000
      },
      "human_input_mode": "NEVER"
    },
    "code_reviewer": {
      "name": "CodeReviewer",
      "system_message": "You are a code reviewer. Review code for bugs, security issues, and best practices.",
      "llm_config": {
        "config_list": [
          {
            "model": "gpt-4",
            "api_key": "${OPENAI_API_KEY}",
            "api_type": "openai"
          }
        ],
        "temperature": 0.3
      },
      "human_input_mode": "NEVER"
    }
  },
  "group_chats": {
    "coding_team": {
      "name": "CodingTeam",
      "agents": ["assistant", "code_reviewer", "user_proxy"],
      "max_round": 15,
      "speaker_selection_method": "round_robin"
    },
    "planning_team": {
      "name": "PlanningTeam",
      "agents": ["assistant", "user_proxy"],
      "max_round": 10,
      "speaker_selection_method": "auto"
    }
  },
  "tools": {
    "file_search": {
      "name": "file_search",
      "description": "Search for files in the workspace",
      "parameters": {
        "query": {
          "type": "string",
          "description": "Search query"
        },
        "max_results": {
          "type": "integer",
          "description": "Maximum number of results to return"
        }
      }
    },
    "code_analyzer": {
      "name": "code_analyzer",
      "description": "Analyze code for quality and issues",
      "parameters": {
        "file_path": {
          "type": "string",
          "description": "Path to the file to analyze"
        },
        "rules": {
          "type": "array",
          "description": "List of rules to check"
        }
      }
    }
  }
}
